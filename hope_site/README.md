# Moved

Hope’s **GitHub Pages** files now live in **`docs/`** at the project root (`docs/index.html`, etc.), because GitHub Pages can deploy from the `/docs` folder on `main`.

Do not add new site files here — use **`docs/`** instead.
